# Archived verification-lab prototype

This directory preserves the repository's earlier verification-lab prototype.

The active repository direction has changed: this project is now a project-driven
slide board for `walkinglabs/learn-harness-engineering`. Active work should live
under `modules/` and `docs/`, not under this archive.

Archived material includes:

- the old Chapter 01 verification-lab docs and fixtures;
- deterministic validators and smoke wrappers;
- tests and project-board notes from the verification-lab phase.

This archive is retained only for reference. Do not treat these files as the
current roadmap, public entrypoint, or implementation target.
